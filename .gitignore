.vercel
node_modules
.next
out
.npm-cache
.vercel-home
.env.local
.env*.local

# OS / IDE / build artifacts
.DS_Store
tsconfig.tsbuildinfo

# Claude Code local tooling state (worktrees, launch config, etc.)
.claude/launch.json
.claude/worktrees/
